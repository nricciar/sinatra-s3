source :gemcutter

group :production do
  gem 'pg'
end

group :test do
  gem 'sqlite3'
end

gem 'rails', '~> 3.0.0', :require => false
gem 'sinatra'
gem 'haml'
gem 'aws-s3', :require => 'aws/s3'
gem 'sinatra-reloader'
